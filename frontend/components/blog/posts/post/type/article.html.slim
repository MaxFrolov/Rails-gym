.post-head__header.article__header
  h2 {{currentPost.title}}
  .post_main__img
    img.img-responsive src="{{currentPost.image.origin}}"
    .post-likes__wrapper
      likes target-type="post" current-user="currentUser" resource="currentPost" user-liked="userLiked"
  h5 {{currentPost.subtitle}}